RSpec.describe Manageiq::File::Storage do
  it "has a version number" do
    expect(Manageiq::File::Storage::VERSION).not_to be nil
  end

  it "does something useful" do
    expect(false).to eq(true)
  end
end
